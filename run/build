#!/usr/bin/env bash
# run/build — compile production firmware
set -euo pipefail
source "$(dirname "$0")/lib.sh"
echo "[build] compiling $ENV_PROD"
(cd "$PIO_DIR" && "$PIO" run -e "$ENV_PROD")
echo "[build] done"
