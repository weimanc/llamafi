#pragma once
// taskbar.h — 45×240 px vertical icon strip on the right edge (M-MULTIAPP, TASK-083 step 3).
// Stubs only; rendering and hit-test wired up in M-MULTIAPP implementation.

// renderTaskbar() — draw background, icon glyphs, active indicator.
// Called from repaintChrome() on startup and on app switch.
inline void renderTaskbar() {
    // stub — no-op until M-MULTIAPP implementation
}
